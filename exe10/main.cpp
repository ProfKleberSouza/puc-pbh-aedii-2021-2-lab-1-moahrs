#include <stdio.h>
#include "functions.h"

int main() {

  int a;
  
  // ENTRADA DE DADOS
  scanf("%d", &a);

  // PROCESSAMENTO - EXECUTANDO A FUNCAO
  tabela_verdade(a);

  return(0);
}